{
  "name": "kiendt-website",
  "version": "1.0.0",
  "description": "Web using firebase",
  "homepage": "https://kiendt.github.io/kiendt-cv",
  "main": "index.js",
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "kiendt",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
